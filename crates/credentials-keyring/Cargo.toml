[package]
name = "credentials-keyring"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "credentials_keyring"
path = "src/lib.rs"

[dependencies]
keyring = "4.1.6"
secrecy = "0.10.3"
ssh-manager-core = { version = "0.3.0", path = "../core" }
